# 改修済メモ

### コードレビュー対応（2026-04-18）
- `gui.py`：`FONT_FAMILY = "Meiryo UI"` 定数を追加、全フォント指定を定数参照に変更
- `gui.py`：`CardWidget` dataclass を追加（`frame: tk.Frame`, `label: tk.Label`）
- `gui.py`：`self.cards` を `list[CardWidget]` に変更
- `gui.py`：`draw_hand` のシグネチャを `draw_hand(deck, n=13)` に変更（デッキをキャッシュ）
- `gui.py`：`App.__init__` で `self._deck = idols * 3 + ATTRIBUTE_TILES` をキャッシュ
- `gui.py`：`draw_hand` にデッキ枚数不足チェック（`ValueError`）を追加
- `gui.py`：`load_idols` に CSV 未検出チェック（`FileNotFoundError`）を追加
- `gui.py`：`load_idols` の `int()` 変換を `try/except ValueError` で保護
- `gui.py`：`_draw` の `enumerate` + インデックス参照を `zip` に変更
- `gui.py`：属性牌コメントを `# 属性牌 3種 × 各1枚 = 3枚` に修正

### デザイン改修（2026-04-18）
- `gui.py`：フォント全箇所を `Yu Gothic UI` → `Meiryo UI` に変更
- `gui.py`：カードレイアウトを7列×2行 → 1行13列に変更
- `gui.py`：カードサイズを縦長に変更（78×80 → 56×100px）
- `gui.py`：番号表示ラベル削除
- `gui.py`：名前表示を表示名（名前のみ）に変更 → `idol[2]` を使用
- `idols.csv`：3列目（表示名＝名前のみ）を全52行に追加

### 機能追加（2026-04-18）②
- `gui.py`：`HIGHLIGHT_BORDER = "#ffd700"` 定数追加
- `gui.py`：`check_tenpai(hand, units)` 追加（1枚欠けユニットを返す）
- `gui.py`：`get_contributing_ids(hand, units)` 追加（完成・テンパイユニットの関与IDセットを返す）
- `gui.py`：`App.__init__` に `self._hand12`, `self._id_to_name` 追加
- `gui.py`：`_build_ui` に `self.tenpai_label` 追加、ボタンを `self._draw_btn` として保持
- `gui.py`：`_draw` をフェーズディスパッチャーに変更
- `gui.py`：`_draw_12` 追加
  - デッキから12枚ドロー
  - 役構成アイドルを金枠（`HIGHLIGHT_BORDER`、`highlightthickness=4`）・左寄せ表示
  - 役とテンパイを別ラベルに表示
  - ボタンを「13枚目を引く」に変更
- `gui.py`：`_draw_13` 追加
  - 残りデッキから1枚ドロー
  - 13枚目スロットに表示
  - 12枚完成役 / 13枚目で完成した役を区別してラベル表示
  - 13枚手牌でのテンパイ役を「惜しい:」ラベルで表示
  - ボタンを「ドロー」に戻す

### デザイン改修（2026-04-18）④
- `gui.py`：カードサイズを56×100 → 112×200px（2倍）に変更
- `gui.py`：フォントをスケールアップ（カード内:8→14、タイトル:18→28、サブタイトル:9→14、ボタン:13→20）
- `gui.py`：`wraplength` を50→100に変更
- `gui.py`：カード間`padx`を3→5に変更
- `gui.py`：13枚目のカードをcolumn 13に配置し、column 12に1枚分の隙間を確保（`columnconfigure minsize=122`）

### 機能追加（2026-04-18）
- `units.csv`：ユニット定義CSV新規作成（書式: `ユニット名,ID1,ID2,...`）
  - 765プロオールスターズ（ID:1-13）を初期定義として記載
- `gui.py`：`UNITS_PATH` 定数を追加
- `gui.py`：`load_units()` 関数を追加（units.csv読み込み、`{ユニット名: frozenset(ID)}`返却）
- `gui.py`：`check_yaku(hand, units)` 関数を追加（成立ユニット名リスト返却）
- `gui.py`：`App.__init__` で `self._units = load_units()` をキャッシュ
- `gui.py`：`_build_ui` に `self.yaku_label` を追加（ドロー後の役表示エリア）
- `gui.py`：`_draw` で `check_yaku` を呼び出し、役ラベルを更新

### デザイン改修（2026-04-18）③
- `gui.py`：青羽美咲（ID:53）の枠を緑色 `#4caf50` に設定（`SPECIAL_BORDER` 定数）
- `gui.py`：全アイドルの属性枠色対応
  - `ATTR_BORDER`（ID→色）廃止 → `ATTR_COLORS`（属性名→色）+ `SPECIAL_BORDER`（ID→色）に分離
  - `ATTRIBUTE_TILES` を3タプル → 4タプル（属性フィールド追加）
  - `load_idols` を4タプル読み込みに変更（4列目なければ空文字）
  - `_draw` を `SPECIAL_BORDER.get(id) or ATTR_COLORS.get(attribute)` ロジックに変更
- `idols.csv`：4列目（属性）を追加（現在空欄、ユーザーが記入予定）

### デザイン改修（2026-04-18）②
- `gui.py`：属性牌の枠色を属性ごとに変更
  - Princess（101）→ ピンク `#ff69b4`
  - Fairy（102）→ 青 `#64b5f6`
  - Angel（103）→ 黄 `#ffd54f`
  - `ATTR_BORDER` 定数（ID→色マップ）を追加
  - フレームを `relief="groove", bd=2` → `highlightbackground, highlightthickness=2, relief="flat"` に変更
  - `_draw` でIDに応じた `highlightbackground` を適用

### デザイン改修（2026-04-18）⑤
- `gui.py`：`TENPAI_BORDER = "#ffa500"` 定数追加
- `gui.py`：`get_contributing_ids` 削除 → `get_yaku_ids` / `get_tenpai_ids` に分割
  - `get_yaku_ids`：完成ユニット (`members <= hand_ids`) のIDセット返却
  - `get_tenpai_ids`：テンパイ（1枚欠け）ユニットの関与IDセット返却
- `gui.py`：`_apply_card_style(card, tile_id, attribute, is_yaku, is_tenpai)` メソッド追加
  - 役: 外枠=金、帯=属性色、内容=BG_CARD（thickness=3）
  - テンパイ: 外枠=オレンジ、帯=属性色、内容=BG_CARD（thickness=3）
  - その他: 外枠=属性色、内容=BG_CARD（thickness=2）
- `gui.py`：`label.pack(expand=True)` → `label.pack(expand=True, padx=4, pady=4)` に変更（帯表示のため）
- `gui.py`：`_draw_btn`（1ボタン）廃止 → `_draw12_btn` + `_draw13_btn` の2ボタン横並びに変更
  - 初期: ドローボタンのみ有効
  - ドロー後: ドロー無効・13枚目有効に切り替え
  - 13枚目引き後: ドロー有効・13枚目無効に戻す
- `gui.py`：`_draw` メソッド削除（2ボタン直接バインドに変更）
- `gui.py`：`_draw_12` を新スタイル関数に変更（役・テンパイ別色・ボタン状態制御）
- `gui.py`：`_draw_13` を全カード再スタイル適用に変更（13枚目含む役・テンパイ色適用）

### デザイン・機能改修（2026-04-19）
- `gui.py`：`check_tenpai` / `get_yaku_ids` / `get_tenpai_ids` / `check_yaku` 削除
- `gui.py`：`assign_yaku_tenpai_indices(hand, units)` 追加
  - ID → hand内インデックスリストで重複コピーを管理
  - 完成ユニット: 残コピーを消費してインデックスを yaku_idx に割り当て
  - テンパイ: ちょうど1メンバー不在かつ残コピーが揃う場合に tenpai_idx に割り当て
  - 戻り値: `(yaku_idx, tenpai_idx, yaku_unit_map, completed, tenpai)`
- `gui.py`：`_apply_card_style` 変更
  - 役: `frame.bg=HIGHLIGHT_BORDER`（金帯）, `label.bg=BG_MAIN`, `highlightthickness=5`
  - テンパイ: `frame.bg=attr_color`, `highlightbackground=TENPAI_BORDER`, `highlightthickness=2`
  - 通常: `frame.bg=BG_CARD`, `highlightbackground=attr_color`, `highlightthickness=2`
- `gui.py`：`yaku_label` / `tenpai_label` に `anchor="w", wraplength=1650` を追加、`.pack(fill="x", padx=24)` に変更
- `gui.py`：`__init__` でジオメトリロック（`update_idletasks` + `geometry` で幅固定）
- `gui.py`：`_draw13_btn` 初期 `state=tk.DISABLED` を削除（両ボタン常時有効化）
- `gui.py`：`_draw_12` 変更
  - `assign_yaku_tenpai_indices` で役/テンパイ判定
  - 役グループ左寄せ（ユニット番号→ID順）、テンパイ非左寄せのソートに変更
  - ボタン state 変更コード削除
- `gui.py`：`_draw_13` 変更
  - `self._hand12 is None` 時に12枚自動ドロー（13枚目ボタン単独で動作可能）
  - `assign_yaku_tenpai_indices` でインスタンス別スタイル適用
  - ボタン state 変更コード削除

### システム改修（2026-04-18）
- `idols.csv`：青羽美咲（53）を追加
- `gui.py`：属性牌3種（Princess/Fairy/Angel）を各1枚デッキに追加（`ATTRIBUTE_TILES`定数）
- `gui.py`：`draw_hand` でアイドル牌159枚+属性牌3枚の合計162枚デッキに変更
- `gui.py`：説明文を「53種×3枚+属性牌3枚のデッキから13枚ドロー」に更新

### 実装内容（2026-04-18）
- `gui.py`：tkinter製GUIアプリ本体
  - ミリオンライブ52アイドル × 3枚 = 156枚デッキ
  - ドローボタンで13枚をランダム抽出・番号順ソートで表示
  - カード表示：7列×2行レイアウト（78×80px）
  - ダークテーマ（#1a1a2e背景 / #e94560アクセント）
- `idols.csv`：アイドル52名の番号・名前データ
- `改修予定メモ.txt`：未着手/完了セクションのみ（空）
